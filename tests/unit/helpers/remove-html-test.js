import { removeHtml } from '../../../helpers/remove-html';
import { module, test } from 'qunit';

module('Unit | Helper | remove html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = removeHtml(42);
  assert.ok(result);
});
