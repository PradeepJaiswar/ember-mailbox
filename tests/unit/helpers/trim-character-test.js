import { trimCharacter } from '../../../helpers/trim-character';
import { module, test } from 'qunit';

module('Unit | Helper | trim character');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trimCharacter(42);
  assert.ok(result);
});
